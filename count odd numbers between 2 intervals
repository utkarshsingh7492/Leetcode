class Solution {
public:
    int countOdds(int low, int high) {
        if(low%2==0 && high%2==0){               //condition for checking the high and low position if they are odd
            return (high-low)/2;
        }
        else{
            return (high-low)/2+1;    // formula to remember 
        }
       
    }
};
